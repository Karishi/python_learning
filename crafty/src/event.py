from player import Player

class Event:
    def __init__(self, time) -> None:
        self.time_value = time
    
    def event_effect(event_type):
        if event_type is "energy":
            pass #FIX LATER